import 'package:flutter/material.dart';

class Theme2 {
  const Theme2();

  static const gradient = const LinearGradient(
    colors: [Color(0xff57CBE6), Color(0xff5649E6)],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    stops: [0.0, 1.0],
  );
}
